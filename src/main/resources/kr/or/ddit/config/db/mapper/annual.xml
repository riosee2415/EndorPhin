<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="annual">
   
   	<!-- 연차정보 전체 가져오기 -->
   	<select id="getAllAnnuala" resultType="annualVo">
		SELECT
		    USERID, 
		    ANNUALDATE, 
		    ANNUALSTART, 
		    ANNUALEND, 
		    OCCURANNUAL, 
		    USERDATE
		FROM ANNUAL
   	</select>

	<!-- 사원 아이디 별 연차정보 전체 가져오기 -->
 	<select id="getAllAnnual" resultType="annualVo" >
		SELECT
		    USERID, 
		    ANNUALDATE, 
		    ANNUALSTART, 
		    ANNUALEND, 
		    OCCURANNUAL, 
		    USERDATE
		FROM ANNUAL
		WHERE userId = #{userId}
   	</select>
   
   	<!-- 연차정보 추가하기 -->
   	<insert id="insertAnnual" parameterType="annualVo">
   		INSERT INTO ANNUAL
		    (USERID, ANNUALDATE, ANNUALSTART, ANNUALEND, OCCURANNUAL, USERDATE)
		VALUES
		    (#{userId},#{annualDate},#{annualStart},#{annualEnd},#{occurannual},#{userDate})
   	</insert>
   
   
   	<!-- 사원아이디 별 연차정보 삭제하기
	파라미터 타입 		: map 
	파라미터 key값 	: userId
	-->
   	<delete id="deleteAnnual" parameterType="map">
  		DELETE 
  		FROM ANNUAL
  		WHERE USERID = #{userId}
   	</delete>
   	
   	
   	<!-- 사원아이디 별 연차정보 수정하기
	파라미터 타입 		: map 
	파라미터 key값 	: occurannual, userId
	-->
   	<update id="updateAnnual" parameterType="map">
   		UPDATE ANNUAL
   		SET OCCURANNUAL = #{occurannual}
   		WHERE USERID = #{userId}
   	</update>
   
	
   
   
   
</mapper>



