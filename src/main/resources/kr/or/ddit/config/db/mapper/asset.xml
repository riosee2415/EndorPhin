<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="asset">

	<!-- 자산 조회하기 -->
	<select id="getAllAsset" resultType="assetVo">
        SELECT
	        ASSETCODE, 
	        ASSETNAME, 
	        acquisitionDate, 
	        ACCOUNTNAME, 
	        CLIENTNAME, 
	        SANGGAKWAY, 
	        ACQUISITIONPRICE, 
	        SLIPNUMBER, 
	        RESIDUALVALUE, 
	        UNIT, 
	        JUKYO, 
	        UNITPRICE, 
	        QUANTITY, 
	        COMPANYCODE,
	        sanggakCode,
	        depreciation,
	        purchaseCode
	    FROM ASSET
	</select>
	
	
	<!-- 자산 추가하기 -->
	<insert id="insertAsset" parameterType="assetVo">
		INSERT INTO ASSET
        (ASSETCODE, ASSETNAME, ACQUISITIONDATE, ACCOUNTNAME, CLIENTNAME, SANGGAKWAY, acquisitionPrice, 
          RESIDUALVALUE, UNIT, JUKYO, UNITPRICE, QUANTITY,sanggakCode,depreciation,purchaseCode)
   		 VALUES
        (#{assetCode},#{assetName},#{acquisitionDate},#{accountName},#{clientName},#{sanggakWay},
        #{acquisitionPrice},#{residualvalue},#{unit},#{jukyo},#{unitPrice},#{quantity},#{sanggakCode},#{depreciation},#{purchaseCode})
	
	</insert> 
	
	<insert id="insertStatusAsset" parameterType="assetVo">              
     insert into asset
        (assetCode, acquisitionDate,sanggakWay, purchaseCode,acquisitionPrice) values
        (#{assetCode},#{acquisitionDate},#{sanggakWay},#{purchaseCode},#{acquisitionPrice}) 
	</insert>
	
	<update id="updateAsset" parameterType="assetVo">
		 update asset set 
					assetName  = #{assetName},
					acquisitionDate = #{acquisitionDate},
					accountName = #{accountName},
					clientName = #{clientName},
					sanggakWay  = #{sanggakWay},
					acquisitionPrice = #{acquisitionPrice},
					slipNumber = #{slipNumber},
					residualvalue = #{residualvalue},
					unit = #{unit},
					jukyo = #{jukyo},
					unitPrice = #{unitPrice},
					quantity = #{quantity},
					sanggakCode = #{sanggakCode},
					depreciation = #{depreciation},
					purchaseCode = #{purchaseCode}
		     where  assetCode = #{assetCode}
	</update>
</mapper>



