<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="product">

	<!-- 상품 입력 -->
	<insert id="insertProduct" parameterType="productVo">
		insert into product
		(productcode, productname, standard, outline, usestatus, baseprice)
		values
		('P'||#{productCode}
		,#{productName}
		,#{standard}
		,nvl(#{outLine, jdbcType=VARCHAR},null)
		,nvl(#{usestatus, jdbcType=VARCHAR},1)
		,#{basePrice})
	</insert>
	

	<!-- 상품  전체 불러오기 -->
	<select id="getAllProduct" resultType="productVo">
		select
		productcode, productname, standard, outline, usestatus, baseprice
		from product
	</select>


	<!-- 상품 선택하여 가져오기 -->
	<select id="selectProduct" resultType="productVo"
		parameterType="map">
		select
		productcode, productname, standard, outline, usestatus, baseprice
		from
		product where productcode = #{productCode}
	</select>


	<!-- 상품 삭제 -->
	<delete id="deleteProduct" parameterType="map">
		delete from product
		where productcode = #{productcode}
	</delete>

</mapper>



