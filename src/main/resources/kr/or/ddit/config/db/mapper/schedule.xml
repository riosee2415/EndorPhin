<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="schedule">


	<insert id="insertSchedule" parameterType="scheduleVo">
		insert into schedule
		(schedule_no, schedule_title, schedule_start, schedule_end)
		values
		(schedule_seq.nextval,#{schedule_title},to_date(#{schedule_start},'yyyy-mm-dd'),to_date(#{schedule_end},'yyyy-mm-dd')-1)
	</insert>

	
	<select id="getAllSchedule" resultType="scheduleVo">
		select
		schedule_no, schedule_title, to_char(schedule_start,'yyyy-mm-dd') schedule_start, to_char(schedule_end,'yyyy-mm-dd') schedule_end
		from schedule
	</select>


	
 	<!-- 전표상세 수정하기 -->
	<!-- <update id="updateSales_detail" parameterType="sales_detailVo">
		update sales_detail
		set	ESTABLISHCODE = #{establishCode}, 
			JUKYO = #{jukyo}, 
			PRICE = #{price}, 
			STATUS = #{status}
		WHERE SALESDETAILCODE=#{salesDetailCode}
		
	</update>


	


	매입매출  선택하여 가져오기
	<select id="selectSales_detail" resultType="sales_detailVo"
		parameterType="String">
		select
		salesDetailCode, status, price, establishCode, salesCode, jukyo
		from
		sales_detail
		where salesCode = #{salesCode}
	</select>


	매입매출상세  삭제
	<delete id="deleteSales_detail" parameterType="String">
		delete from sales_detail
		where salesCode = #{salesCode}
	</delete>  -->

</mapper>



